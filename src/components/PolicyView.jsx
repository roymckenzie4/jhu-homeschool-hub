/**
 * PolicyView — the State policies view: a regulation heatmap with multi-state
 * comparison.
 *
 * Stub for now (tab-shell scaffolding). Built out in subsequent steps:
 * categorical choropleth shaded by regulation count, multi-select with a
 * "Comparing" chip row (cap of 6), and a side-by-side comparison table.
 */

export default function PolicyView() {
  return (
    <div className="py-16 text-center font-sans text-sm text-sable/50">
      State policies view — coming up.
    </div>
  );
}
